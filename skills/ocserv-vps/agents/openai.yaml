interface:
  display_name: "ocserv VPS releases"
  short_description: "Safe ocserv release deploys and rollbacks"
  default_prompt: "Use $ocserv-vps to inspect a Debian or Ubuntu VPS and deploy a version-pinned, signed ocserv release with validation and automatic rollback."

policy:
  allow_implicit_invocation: false
