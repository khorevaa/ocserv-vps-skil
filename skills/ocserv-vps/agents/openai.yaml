interface:
  display_name: "ocserv VPS and UI"
  short_description: "Deploy and manage Dockerized ocserv with UI"
  default_prompt: "Use $ocserv-vps to deploy or maintain a verified Dockerized ocserv VPN and its secure management UI with ACME, firewall, health checks, and rollback."

policy:
  allow_implicit_invocation: false
