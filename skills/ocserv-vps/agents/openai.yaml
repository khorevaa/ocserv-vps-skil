interface:
  display_name: "ocserv VPS deployment"
  short_description: "Deploy and operate ocserv on a VPS"
  default_prompt: "Use $ocserv-vps to deploy ocserv on my VPS and install the private UI only when I ask for it."

policy:
  allow_implicit_invocation: false
